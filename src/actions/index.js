export const selectLibrary = (libraryId) => {
    return {
        type: 'user_transaction',
        payload: libraryId
    };
};

export const selectLibrary = (libraryId) => {
    return {
        type: 'catagory',
        payload: libraryId
    };
};

export const selectLibrary = (libraryId) => {
    return {
        type: 'remaining_amount',
        payload: libraryId
    };
};
